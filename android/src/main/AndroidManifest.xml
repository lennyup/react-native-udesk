<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lenny.module.udesk">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application android:allowBackup="true">
        <meta-data android:name="UDESK_DOMAIN" android:value="${UDESK_DOMAIN}"/>
        <meta-data android:name="UDESK_APPKEY" android:value="${UDESK_APPKEY}"/>
        <meta-data android:name="UDESK_APPID" android:value="${UDESK_APPID}"/>
    </application>

</manifest>
